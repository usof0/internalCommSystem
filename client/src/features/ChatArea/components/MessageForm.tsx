

type MessageFormProps = {
    className?: string;
}

function MessageForm({ className }: MessageFormProps) {
    return (
        <form
            className={className}
            action="#"
        >

        </form>
    );
}

export default MessageForm;
