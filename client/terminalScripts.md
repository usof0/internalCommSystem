## To use JSON file as a server api
```npm install -g json-server```
```json-server --watch db.json --port 3030```

### To access any of json items we add the keys for the URL
ex: ```localhost:3030/posts```