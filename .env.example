POSTGRES_USER=ics
POSTGRES_PASSWORD=change_me_db_password
POSTGRES_DB=icsdb

HTTP_PORT=80

# Same-origin defaults are recommended when the client is served by the bundled Nginx.
VITE_API_BASE_URL=/api/v1
VITE_SOCKET_URL=
